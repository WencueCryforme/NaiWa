# 贡献指南

## 基本流程

fork本仓库 -> 克隆fork仓库到本地 -> 本地做出修改 -> 本地提交变更并提交到云端fork仓库 -> 创建PR -> 等待审核

> 不要擅自修改 `.gitignore` 文件，不要把你本地的工作残留提交到仓库。

## 新增表情包

- 所有表情包按专辑分类存放，按照目录规范，应该在 `dist/meme/` 下新建专辑文件夹。
- 在你的专辑文件夹下存放 **压缩版本** 的 `.png` 或 `.gif` 文件(不大于500KB)，文件名称要简单描述表情包内容或语义。
- 提交PR到本仓库，并在PR评论区附上你的专辑文件 **未压缩版本** 的 `.zip` 压缩包(命名为专辑名称)，然后等待审核。
- 审核通过后，你的表情包专辑将被同步上传到某个版本Release的Assets列表中，并且在 [表情包归档清单文档](docs/meme-raw-list.md) 中被列出。

> 压缩图片建议使用仓库内的 `scripts/img_to_png.py` 脚本。

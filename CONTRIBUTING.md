# 贡献指南

[简体中文] |
[English](./CONTRIBUTING_en-US.md)


## 基本流程

fork 本仓库 -> 克隆 fork 仓库到本地 -> 本地做出修改 -> 本地提交变更并提交到云端 fork 仓库 -> 创建 PR -> 等待审核

> 不要擅自修改 `.gitignore` 文件,不要把你本地的工作残留提交到仓库


## 新增表情包

- 所有表情包按专辑分类存放,按照目录规范,在 `dist/meme/` 下新建专辑文件夹
- 在专辑文件夹下存放 **已压缩版本** 的 `.jpg` (不大于500KB为宜) 或 `.gif` (不大于2MB为宜) 文件,文件名称要简单描述表情包内容或语义
- 提交 PR 到本仓库,并在 PR 评论区附上专辑文件 **未压缩版本** 的 `.zip` 压缩包(命名为专辑名称),然后等待审核
- 审核通过后,表情包专辑的未压缩版本和压缩版本都将被同步上传到某个版本 Release 的 Assets 列表,并在 [表情包归档清单文档](docs/meme-list.md) 中列出该表情包专辑
- 或者你可以直接按照 [#补充表情包高清版本](#补充表情包高清版本) 段落的流程提交你的 **未压缩版本** 表情包的 `.zip` 压缩包,不过这将花费更长时间等待审核和分类处理

> 压缩表情包建议使用仓库提供的脚本 `scripts/img_resize.py` 或 `scripts/gif_resize.py`


## 补充表情包高清版本

当你持有某些已收录表情包的 **高清版本** 时,如果你的版本比仓库 Release 已有的 **raw** 级别表情包文件更高清,你可以通过以下规范流程贡献它们:
- 按照如下层级结构打包你的表情包进 `.zip` 压缩包
```
压缩包.zip
raw
└─ 专辑名称
   └─ 表情包文件
```
- 将压缩包规范地重命名为 `update.raw.meme.<专辑名称>-yyyy-MM-dd.zip` ,其中 `yyyy-MM-dd` 应替换为打包时的 **UTC时间的日期**
- [新建一个Issue] 并附上你的表情包 `.zip` 压缩包


## 下架表情包

如果已收录的某些表情包在你认为或实际上确实存在 **抄袭、侵权、违反法律** 等状况,你可以申请下架:
- [新建一个Issue] 提出下架申请,并附上你的理由和这些表情包所在的仓库路径
- 如果 Issue 评论认可你的理由,或者那些表情包的不合规情况显而易见,仓库将尽快下架它们
- 下架范围仅限 **删除对应Release附带的归档压缩包、新增移除对应表情包的仓库commit** ,但用户仍能通过回溯仓库git历史找回它们的压缩版本
- 对于该部分表情包已造成的在现实、网络、社交媒体等方面的负面影响,**由表情包文件的提供者负责**,本仓库作者及社区 **不承担** 任何的连带责任

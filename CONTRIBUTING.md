# 贡献指南

[简体中文] |
[English](./CONTRIBUTING_en-US.md)

## 基本流程

fork 本仓库 -> 克隆 fork 仓库到本地 -> 本地做出修改 -> 本地提交变更并提交到云端 fork 仓库 -> 创建 PR -> 等待审核

> 不要擅自修改 `.gitignore` 文件,不要把你本地的工作残留提交到仓库

## 新增表情包

- 所有表情包按专辑分类存放,按照目录规范,在 `dist/meme/` 下新建专辑文件夹
- 在专辑文件夹下存放 **压缩版本** 的 `.png` 或 `.gif` 文件(不大于 500KB),文件名称要简单描述表情包内容或语义
- 提交 PR 到本仓库,并在 PR 评论区附上专辑文件 **未压缩版本** 的 `.zip` 压缩包(命名为专辑名称),然后等待审核
- 审核通过后,表情包专辑将被同步上传到某个版本 Release 的 Assets 列表中,并在 [表情包归档清单文档](docs/meme-raw-list.md) 中列出

> 压缩图片建议使用仓库内的 `scripts/img_to_png.py` 脚本